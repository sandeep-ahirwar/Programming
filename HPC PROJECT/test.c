#include <stdio.h>

int main()
{
    int a = 0;
    for (int o = 0; o < 5; o++)
    {
        for (int g = 0; g < 6; g++)
        {
            printf("%d, %d \n", o, g);
        }
    }
    return 0;
}